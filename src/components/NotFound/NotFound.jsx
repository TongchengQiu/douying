'use strict';
// require('./NotFound.scss');

var NotFound = React.createClass({
  render: function() {
    return (
      <div className="not-found">
        NotFound
      </div>
    );
  }
});

module.exports = NotFound;
